#include <stdio.h>

int main()
{
	int time, hour, minute;

	printf("계산할 초를 입력: ");
	scanf("%d", &time);

	hour = time / 3600;
	time = time % 3600;
	minute = time / 60;
	time = time % 60;

	printf("시간:%d  \n", hour);
	printf("분: %d \n", minute);
	printf("초: %d \n", time);
	
}
