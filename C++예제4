#include <iostream>

int main(void)
{
	int val, result;

	
	for (; ;) {
		std::cout << "이번달 판매 금액 만원단위로(-1 입력하면 종료): ";
		std::cin >> val;

		if (val == -1)
			break;

		result = 50 + val * 0.12;
		std::cout << "이번달 급여: " << result << std::endl;
	}
	std::cout << "프로그램을 종료 ㅎㅎ";
}
