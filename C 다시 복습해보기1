#include <stdio.h>

int main()
{
	int x1, x2, y1, y2, result; //x1,y1:좌상단 x2,y2:우상단 result:연산

	printf("좌 상단의 x,y의 좌표: ");
	scanf("%d %d", &x1, &y1);
	printf("우 상단의 x,y의 좌표: ");
	scanf("%d %d", &x2,&y2);

	result = (x2 - x1)*(y2 - y1);
	printf("직사각형의 두 점이 이루는 직사각형 넓이:%d ",result);

}
