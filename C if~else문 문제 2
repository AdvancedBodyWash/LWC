#include <stdio.h>

int main()
{
	int num1, num2, result;

	printf("두 수의 정수를 입력: "); //두 수 뺄셈
	scanf("%d %d", &num1, &num2);

	if (num1 > num2)		//반드시 큰 수에서 작은수 뺄 수 있게 조건식을 설정
		result = num1 - num2;
	else
		result = num2 - num1;

	printf("뺄셈 결과:%d",result);
}
